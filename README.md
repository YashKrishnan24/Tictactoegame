# 🎮 Tic Tac Toe (CLI Version in Python)

A colorful **command-line Tic Tac Toe game** built using Python.  
This project demonstrates game logic, input validation, and terminal UI enhancement using colors.

Players take turns as **X** and **O**, and the game automatically detects wins and draws.

---

## 🚀 Features

- Two-player turn-based gameplay (X vs O)
- Colored terminal UI using `colorama`
- Automatic win detection (rows, columns, diagonals)
- Draw detection when the board is full
- Input validation for invalid or occupied positions
- Cross-platform screen clearing (Windows & Linux/macOS)

---

## 🛠️ Technologies Used

- Python 3
- `colorama` (for colored terminal output)
- Standard Python libraries (`os`, `time`, `sys`)

---



